spring:
  datasource:
    url: postgresql://neondb_owner:npg_odbn6lY8cNUM@ep-long-sound-az97efqs.c-3.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
    username: neondb_owner
    password: npg_odbn6lY8cNUM
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 3
      minimum-idle: 1
      connection-timeout: 10000



  ai:
    openai:
      api-key: ${GROQ_API_KEY}
      base-url: ${GROQ_BASE_URL:https://api.groq.com/openai}
      chat:
        options:
          model: ${GROQ_MODEL:llama-3.3-70b-versatile}
          temperature: ${TEMPERATURE:0.1}
          max-tokens: ${MAX_TOKENS:7000}
    chat:
      memory:
        repository:
          jdbc:
            initialize-schema: always

cors:
  allowed-origins: ${cors.allowed-origins:http://localhost:3000,http://localhost:5173,https://fastchat-ten.vercel.app,https://fastchat-llm.vercel.app}
  allowed-methods: ${cors.allowed-methods:GET,POST,PUT,DELETE,OPTIONS}

logging:
  level:
    root: INFO
    com.rakeshgupta: DEBUG
    org.springframework.jdbc.core: WARN